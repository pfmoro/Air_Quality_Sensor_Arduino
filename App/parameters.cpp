#include "parameters.h"

// Definição real e inicialização da variável (aloca memória uma única vez)
float correction_factor = 1.0f;

int soil_moisture_dry = 600;  // Ajuste padrão aproximado (Ex: Arduino 5V)
int soil_moisture_wet = 250;  // Ajuste padrão aproximado